export enum ModalRoute {
  PickRaaga = 'PickRaaga',
  PickTaala = 'PickTaala',
}

export enum OverlayRoute {
  Authentication = '/auth',
}

export enum PageRoute {
  Authentication = '/auth',
  Compose = '/compose',
  Feed = '/user/:name',
  Home = '/',
}
