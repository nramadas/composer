export enum ModalRoute {
  PickRaaga = 'PickRaaga',
}

export enum OverlayRoute {
  Authentication = '/auth',
}

export enum PageRoute {
  Authentication = '/auth',
  Compose = '/compose',
  Feed = '/user/:name',
  Home = '/',
}
